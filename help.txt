sudo apt-get update
sudo apt install python3.10-venv
python3 -m venv ./venv
. venv/bin/activate
pip install -e .
pip list
uvicorn main:app
uvicorn main:app --reload
pip install alembic

docker-compose pull (обновить)
docker-compose down (остановить)
docker-compose up -d (запустить)
docker compose exec db psql SabterSite2DB --username Zargerion --password   -> (Sabter1)  -> exit
docker compose exec app alembic upgrade head

docker run -it --name f4ccf3f9ab86 app uvicorn app.main:app --host 0.0.0.0
docker ps -a (все контейнеры)
docker network inspect sabter-site-2_custom
docker run -v 0748b39ebcbcbb86406d37d11b9be9bda03c8e8ccaa472ecf7bfd8b3e0328cf6:/var/lib/postgresql/data postgres
docker cp container_name:/var/lib/postgresql/data ./db

alembic init alembic
alembic revision --autogenerate -m "init"
alembic upgrade head
("alternativly without alembic" -> python3 -> import dependencies -> dependencies._add_tables() -> exit())

__
bd
\d+
\d
\x
\d creations
SELECT * FROM creations;
__

sudo apt-get install npm, nodejs
npm install -g @vue/cli (-g глобально на комп)
vue create .
npm install --save axios@1.2.1 bootstrap@5.2.3 (--save в проект package.json)
npm run serve

---
sudo chmod a+rwx . --recursive (анлочит пермишен лок) (u g c a)
(--recursive еще все вложенное задевает)
ls . -ld